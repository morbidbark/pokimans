[package]
name = "pokimans_server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { workspace = true }
tokio = {workspace = true }
pokimans_common = { path = "../pokimans_common" }
